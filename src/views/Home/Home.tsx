import Spinner from 'src/components/Spinner/Spinner';
import Layout from '../Layout/Layout';

const Home = () => {
  return (
    <Layout>
      <div>
        <Spinner show />
      </div>
    </Layout>
  );
};

export default Home;
